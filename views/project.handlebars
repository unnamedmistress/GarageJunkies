<div class="text-center">
  <h2>{{name}}</h2>
  <img src="../{{photo}}" height = "150px" width = "200px" alt = "project photo">
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{date_created}}</p>
  <p> Located at {{address}} {{city}}, {{state}} {{zip}}</p>
    <span for="img" aria-label="money"></span>
    <span class="dollar-amount">${{price}}</span>
  </p>
  <p>
    <button class="btn btn-primary">Buy Me!</button>
  </p>
</div>